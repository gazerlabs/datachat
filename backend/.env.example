# ===================
# Required
# ===================

# Anthropic API key for Claude access
ANTHROPIC_API_KEY=sk-ant-...

# ===================
# Environment
# ===================

# "development" (default) or "production". Production enables the safety gates:
# DISABLE_AUTH=true is refused, and a non-default ENCRYPTION_KEY is required.
# Set ENV=production on every production deployment.
ENV=development

# ===================
# Authentication (Clerk)
# ===================

# Clerk keys for JWT validation
CLERK_SECRET_KEY=sk_...
CLERK_PUBLISHABLE_KEY=pk_...

# Optional: enforce a specific `aud` claim on every session token. Clerk's
# default tokens don't set `aud`, so leave this unset unless you've configured
# a Clerk JWT template with a custom audience.
# CLERK_JWT_AUDIENCE=datachat-api

# Set to "true" to bypass auth in development (creates an admin dev_user).
# Refused when ENV=production.
DISABLE_AUTH=true

# ===================
# Database
# ===================

# Database connection string (defaults to sqlite:///./datachat.db)
# For production, use PostgreSQL:
DATABASE_URL=postgresql://user:password@host:5432/datachat

# ===================
# Security
# ===================

# Encryption key for warehouse credentials. The default below is only used when
# ENV=development; production deployments MUST set this to a unique value.
# Generate one with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
ENCRYPTION_KEY=dev-encryption-key-change-in-production

# ===================
# CORS
# ===================

# Comma-separated allowed origins. Defaults to FRONTEND_URL when unset.
# Avoid "*" — browsers reject it when combined with credentialed requests,
# and it weakens CSRF posture even when they don't.
ALLOWED_ORIGINS=http://localhost:8080,https://yourdomain.com

# ===================
# Rate limiting + warehouse query limits
# ===================

# Chat endpoint rate limit, per user (or per IP for unauthed). Default 30/minute.
# Format follows slowapi: "<count>/<period>" e.g. "60/minute", "200/hour".
CHAT_RATE_LIMIT=30/minute

# Statement timeout passed to Postgres / Redshift / Snowflake connections.
# Kills long-running queries server-side. Default 60s.
WAREHOUSE_QUERY_TIMEOUT_SECONDS=60

# Max cached warehouse executors before LRU eviction. Each entry holds one
# open DB connection. Override only if you connect to hundreds of distinct
# warehouses from a single backend.
WAREHOUSE_CACHE_MAX_SIZE=256

# ===================
# Email Notifications (Resend)
# ===================

# Resend API key for sending consulting inquiry emails (optional)
RESEND_API_KEY=re_...
NOTIFICATION_EMAIL=hello@datachat.app
RESEND_FROM_EMAIL=datachat <onboarding@resend.dev>

# ===================
# Demo Mode
# ===================

# MotherDuck token and database for public demo (optional)
DEMO_MOTHERDUCK_TOKEN=your-motherduck-token
DEMO_MOTHERDUCK_DATABASE=sample_data

